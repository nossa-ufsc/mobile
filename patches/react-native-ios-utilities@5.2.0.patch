diff --git a/ios/Sources/Extensions+Helpers/RCTView+Helpers.swift b/ios/Sources/Extensions+Helpers/RCTView+Helpers.swift
index 09be306d5aa39337c5114c2ad6ba7513218e0751..46c2f347970e0d9ff3f374b2151acb0342df31e0 100644
--- a/ios/Sources/Extensions+Helpers/RCTView+Helpers.swift
+++ b/ios/Sources/Extensions+Helpers/RCTView+Helpers.swift
@@ -25,17 +25,11 @@ public extension RCTView {
     return rootView.recursivelyFindSubview(whereType: targetType);
   };
   
-  var closestParentReactContentView: RCTRootContentView? {
-    let targetType = RCTRootContentView.self;
-  
-    if let match = self.recursivelyFindParentView(whereType: targetType) {
-      return match;
-    };
-    
-    guard let rootView = self.rootViewForCurrentWindow else { return nil };
-    return rootView.recursivelyFindSubview(whereType: targetType);
-  };
-  
+  // NOTE (nossa-ufsc patch): `closestParentReactContentView` referenced the
+  // legacy-architecture `RCTRootContentView` class, which is not exported by
+  // React Native 0.86's prebuilt React framework and broke linking. This app
+  // runs the New Architecture, where that Paper-only fallback is never used, so
+  // the property is removed and its single caller below returns `nil` instead.
   var reactTouchHandlers: [RCTTouchHandler]? {
     self.gestureRecognizers?.compactMap {
       $0 as? RCTTouchHandler
@@ -57,7 +51,7 @@ public extension RCTView {
       guard let superview = currentView.superview else { break };
       currentView = superview;
     };
-    
-    return closestParentReactContentView?.reactTouchHandlers?.first;
+
+    return nil;
   };
 };
